<PISI>
    <SourceName>Pardus</SourceName>
    <Version>2011</Version>
    <Description xml:lang="tr">Pardus 2011 Deposu</Description>
    <Description xml:lang="en">Pardus 2011 Repository</Description>
    <Type>Core</Type>
    <BinaryName>Pardus</BinaryName>
    <Obsoletes>


        <!--
            ************************************************************
            not gone to binary stable yet, please don't remove this mark
            ************************************************************
        -->

        <Package>bootsplash</Package>
        <Package>bootsplash-theme-pardus</Package>
        <Package>libpthread-stubs</Package>
        <Package>libxkbui</Package>

        <!-- replaced by opencore-amr -->
        <Package>amrnb</Package>
        <Package>amrwb</Package>
        <Package>mpeg4ip</Package>
        <Package>essential</Package>

        <Package>xorg-video-radeonhd</Package>
        <Package>git-emacs</Package>

        <!-- Replaced by doxywizard -->
        <Package>doxygen-gui</Package>

        <!-- Replaced by icon-theme-hicolor -->
        <Package>hicolor-icon-theme</Package>

        <!-- Replaced by python-setuptools -->
        <Package>setuptools</Package>

        <!-- Replaced by python-pyaspects -->
        <Package>pyaspects</Package>

        <!-- Replaced by python-pygobject -->
        <Package>pygobject</Package>

        <!-- Replaced by python-cairo -->
        <Package>pycairo</Package>

        <!-- Replaced by python-PyOpenGL -->
        <Package>PyOpenGL</Package>

        <!-- Replaced by linux-atm -->
        <Package>br2684ctl</Package>

        <!-- Replaced by freeglut -->
        <Package>glut</Package>

        <!-- Replaced by new allegro -->
        <Package>alleggl</Package>

        <!-- Replaced by perl-SGMLSpm -->
        <Package>SGMLSpm</Package>

        <!-- Replaced by ant-docs -->
        <Package>ant-doc</Package>

        <!-- Replaced by ghostscript-docs -->
        <Package>ghostscript-doc</Package>

        <!-- Replaced by libgphoto2-docs -->
        <Package>libgphoto2-doc</Package>

        <!-- Replaced by python-mutagen -->
        <Package>mutagen</Package>

        <!-- Replaced by python-Pygments -->
        <Package>Pygments</Package>

        <!-- Replaced by perl-docs -->
        <Package>perl-doc</Package>

        <!-- Replaced by perl-5.12.1. They are included
        in Perl Package and thus they should be 
        maintained with Perl Package. -->
        <Package>perl-Archive-Tar</Package>
        <Package>perl-Attribute-Handlers</Package>
        <Package>perl-CGI</Package>
        <Package>perl-Compress-Zlib</Package>
        <Package>perl-Digest-Base</Package>
        <Package>perl-Digest-MD5</Package>
        <Package>perl-Exporter</Package>
        <Package>perl-ExtUtils-CBuilder</Package>
        <Package>perl-ExtUtils-ParseXS</Package>
        <Package>perl-File-Temp</Package>
        <Package>perl-Getopt-Long</Package>
        <Package>perl-IO-Compress-Base</Package>
        <Package>perl-IO-Compress-Zlib</Package>
        <Package>perl-IO-Zlib</Package>
        <Package>perl-Locale-Maketext-Simple</Package>
        <Package>perl-Math-BigInt</Package>
        <Package>perl-Math-Complex</Package>
        <Package>perl-Module-Pluggable</Package>
        <Package>perl-MIME-Base64</Package>
        <Package>perl-libnet</Package>
        <Package>perl-PathTools</Package>
        <Package>perl-Pod-Escapes</Package>
        <Package>perl-Pod-Parser</Package>
        <Package>perl-Pod-Simple</Package>
        <Package>perl-Test-Simple</Package>
        <Package>perl-Text-Tabs+Wrap</Package>
        <Package>perl-Time-HiRes</Package>

        <!-- These packages get emacs prefix -->
        <Package>color-theme</Package>
        <Package>mmm-mode</Package>
        <Package>php-mode</Package>

        <!-- These packages are included in emacs -->
        <Package>cedet</Package>
        <Package>emacs-easypg</Package>

        <!-- Obsoleted by rsyslog -->
        <Package>sysklogd</Package>

        <!-- Obsoleted by polkit-qt_1 -->
        <Package>polkit-qt</Package>

        <!--Farsight is obsoleted and plugins are available in gst-plugins-(good,bad)-->
        <Package>gst-plugins-farsight</Package>

        <!-- Replaced by phonon-backend-* -->
        <Package>phonon-xine</Package>
        <Package>phonon-gstreamer</Package>

        <!-- Replaced by python-SQLObject -->
        <Package>SQLObject</Package>

        <!-- Replaced by python-xcb -->
        <Package>xpyb</Package>

        <!-- Replaced by python-pyalsaaudio -->
        <Package>pyalsaaudio</Package>

        <!-- Replaced by python-pyosd -->
        <Package>pyosd</Package>

        <!-- Replaced by python-tagpy -->
        <Package>tagpy</Package>

        <!-- Replaced by python-pyogg -->
        <Package>pyogg</Package>

        <!-- Replaced by python-pyvorbis -->
        <Package>pyvorbis</Package>

        <!-- Replaced by python-pyao -->
        <Package>pyao</Package>

        <!-- Replaced by python-urwid -->
        <Package>urwid</Package>

        <!-- Replaced by python-numeric -->
        <Package>numeric</Package>

        <!-- Replaced by python-pyv4l -->
        <Package>pyv4l</Package>

        <!-- Replaced by python-qt-* -->
        <Package>PyQt-devel</Package>
        <Package>PyQt</Package>
        <Package>PyQt-webkit</Package>
        <Package>PyQt-phonon</Package>
        <Package>PyQt-doc</Package>
        <Package>PyQt-docs</Package>

        <!-- Replaced by glibc-docs -->
        <Package>glibc-doc</Package>

        <!-- Functionality replaced by glib and no one uses this -->
        <Package>eggdbus</Package>

        <!-- Replaced by python-Cheetah -->
        <Package>cheetah</Package>

        <!-- Replaced by python-pyxdg -->
        <Package>python-xdg</Package>
    </Obsoletes>
</PISI>
