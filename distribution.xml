<PISI>
    <SourceName>Pardus</SourceName>
    <Version>2011</Version>
    <Description xml:lang="tr">Pardus 2011 Deposu</Description>
    <Description xml:lang="en">Pardus 2011 Repository</Description>
    <Type>Core</Type>
    <BinaryName>Pardus</BinaryName>
    <Obsoletes>

        <!-- Dropped packages -->
        <Package>PolicyKit</Package>
        <Package>bootsplash-theme-pardus</Package>
        <Package>bootsplash</Package>
        <Package>git-emacs</Package>
        <Package>kernel-module-headers-pae</Package>
        <Package>kernel-module-headers-rt</Package>
        <Package>kernel-pae</Package>
        <Package>kernel-rt</Package>
        <Package>libpthread-stubs</Package>
        <Package>libxkbui</Package>
        <Package>migration</Package>
        <Package>module-pae-alsa-driver</Package>
        <Package>module-pae-broadcom-wl</Package>
        <Package>module-pae-compat-wireless</Package>
        <Package>module-pae-fglrx</Package>
        <Package>module-pae-ndiswrapper</Package>
        <Package>module-pae-nvidia-current</Package>
        <Package>module-pae-nvidia173</Package>
        <Package>module-pae-nvidia96</Package>
        <Package>module-pae-r5u870</Package>
        <Package>module-pae-syntekdriver</Package>
        <Package>module-pae-virtualbox-guest</Package>
        <Package>module-pae-virtualbox</Package>
        <Package>module-pae-vloopback</Package>
        <Package>module-rt-alsa-driver</Package>
        <Package>network-manager</Package>
        <Package>plasmoid-network-applet</Package>
        <Package>system-manager</Package>
        <Package>texlive-documentation-greek</Package>
        <Package>texlive-psutils</Package>
        <Package>xorg-video-radeonhd</Package>


        <!-- These are source package names written here to work around
             a problem in list-urgent-package-dependencies script. -->
        <Package>avahi-bindings</Package>
        <Package>poppler-bindings</Package>
        <Package>python-std-extensions</Package>

        <!-- replaced by opencore-amr -->
        <Package>amrnb</Package>
        <Package>amrwb</Package>
        <Package>mpeg4ip</Package>
        <Package>essential</Package>

        <!-- Replaced by doxywizard -->
        <Package>doxygen-gui</Package>

        <!-- Replaced by icon-theme-hicolor -->
        <Package>hicolor-icon-theme</Package>

        <!-- Replaced by python-setuptools -->
        <Package>setuptools</Package>

        <!-- Replaced by python-pyaspects -->
        <Package>pyaspects</Package>

        <!-- Replaced by python-pygobject -->
        <Package>pygobject</Package>

        <!-- Replaced by python-cairo -->
        <Package>pycairo</Package>

        <!-- Replaced by python-PyOpenGL -->
        <Package>PyOpenGL</Package>

        <!-- Replaced by linux-atm -->
        <Package>br2684ctl</Package>

        <!-- Replaced by freeglut -->
        <Package>glut</Package>

        <!-- Replaced by new allegro -->
        <Package>alleggl</Package>

        <!-- Replaced by perl-SGMLSpm -->
        <Package>SGMLSpm</Package>

        <!-- Replaced by ant-docs -->
        <Package>ant-doc</Package>

        <!-- Replaced by ghostscript-docs -->
        <Package>ghostscript-doc</Package>

        <!-- Replaced by libgphoto2-docs -->
        <Package>libgphoto2-doc</Package>

        <!-- Replaced by python-mutagen -->
        <Package>mutagen</Package>

        <!-- Replaced by python-Pygments -->
        <Package>Pygments</Package>

        <!-- Replaced by perl-docs -->
        <Package>perl-doc</Package>

        <!-- Replaced by cg-docs -->
        <Package>cg-doc</Package>

        <!-- Replaced by perl-5.12.1. They are included
        in Perl Package and thus they should be 
        maintained with Perl Package. -->
        <Package>perl-Archive-Tar</Package>
        <Package>perl-Attribute-Handlers</Package>
        <Package>perl-CGI</Package>
        <Package>perl-Class-ISA</Package>
        <Package>perl-Compress-Raw-Zlib</Package>
        <Package>perl-Compress-Zlib</Package>
        <Package>perl-Digest-Base</Package>
        <Package>perl-Digest-MD5</Package>
        <Package>perl-Exporter</Package>
        <Package>perl-ExtUtils-CBuilder</Package>
        <Package>perl-ExtUtils-ParseXS</Package>
        <Package>perl-File-Temp</Package>
        <Package>perl-Getopt-Long</Package>
        <Package>perl-IO-Compress-Base</Package>
        <Package>perl-IO-Compress-Zlib</Package>
        <Package>perl-IO-Zlib</Package>
        <Package>perl-libnet</Package>
        <Package>perl-Locale-Maketext-Simple</Package>
        <Package>perl-Math-BigInt</Package>
        <Package>perl-Math-Complex</Package>
        <Package>perl-MIME-Base64</Package>
        <Package>perl-Module-Pluggable</Package>
        <Package>perl-PathTools</Package>
        <Package>perl-Pod-Escapes</Package>
        <Package>perl-Pod-Parser</Package>
        <Package>perl-Pod-Simple</Package>
        <Package>perl-Storable</Package>
        <Package>perl-Sys-Syslog</Package>
        <Package>perl-Test-Simple</Package>
        <Package>perl-Text-Tabs+Wrap</Package>
        <Package>perl-Time-HiRes</Package>
        <Package>perl-Time-Local</Package>
        <Package>perl-Time-Piece</Package>

        <!-- These packages get emacs prefix -->
        <Package>color-theme</Package>
        <Package>mmm-mode</Package>
        <Package>php-mode</Package>

        <!-- These packages are included in emacs -->
        <Package>cedet</Package>
        <Package>emacs-easypg</Package>

        <!-- Obsoleted by rsyslog -->
        <Package>sysklogd</Package>

        <!-- Obsoleted by polkit-qt_1 -->
        <Package>polkit-qt</Package>

        <!--Farsight is obsoleted and plugins are available in gst-plugins-(good,bad)-->
        <Package>gst-plugins-farsight</Package>

        <!-- Replaced by phonon-backend-* -->
        <Package>phonon-xine</Package>
        <Package>phonon-gstreamer</Package>

        <!-- Replaced by python-SQLObject -->
        <Package>SQLObject</Package>

        <!-- Replaced by python-xcb -->
        <Package>xpyb</Package>

        <!-- Replaced by python-pyalsaaudio -->
        <Package>pyalsaaudio</Package>

        <!-- Replaced by python-pyosd -->
        <Package>pyosd</Package>

        <!-- Replaced by python-tagpy -->
        <Package>tagpy</Package>

        <!-- Replaced by python-pyogg -->
        <Package>pyogg</Package>

        <!-- Replaced by python-pyvorbis -->
        <Package>pyvorbis</Package>

        <!-- Replaced by python-pyao -->
        <Package>pyao</Package>

        <!-- Replaced by python-urwid -->
        <Package>urwid</Package>

        <!-- Replaced by python-numeric -->
        <Package>numeric</Package>

        <!-- Replaced by python-pyv4l -->
        <Package>pyv4l</Package>

        <!-- Replaced by python-qt-* -->
        <Package>PyQt-devel</Package>
        <Package>PyQt</Package>
        <Package>PyQt-webkit</Package>
        <Package>PyQt-phonon</Package>
        <Package>PyQt-doc</Package>
        <Package>PyQt-docs</Package>

        <!-- Replaced by glibc-docs -->
        <Package>glibc-doc</Package>

        <!-- Functionality replaced by glib and no one uses this -->
        <Package>eggdbus</Package>

        <!-- Replaced by python-Cheetah -->
        <Package>cheetah</Package>

        <!-- Replaced by python-pyxdg -->
        <Package>python-xdg</Package>

        <!-- Replaced by python-pychecker -->
        <Package>pychecker</Package>

        <!-- Replaced by python-pylint -->
        <Package>pylint</Package>

        <!-- Renamed to plasma-applet-service-manager -->
        <Package>plasmoid-service-applet</Package>

        <!-- Replaced by icon-theme-gnome -->
        <Package>gnome-icon-theme</Package>

        <!-- Replaced by xz -->
        <Package>lzma</Package>

        <!-- Replaced by microcode-ctl -->
        <Package>microcode-data</Package>

        <!-- Replaced by python-pycrypto -->
        <Package>pycrypto</Package>

        <!-- Replaced by python-papyon -->
        <Package>papyon</Package>

        <!-- Replaced by python-gsl -->
        <Package>pygsl</Package>

        <!-- Replaced by ipheth-pair -->
        <Package>module-ipheth-driver</Package>
        <Package>module-ipheth-driver-userspace</Package>
        <Package>module-pae-ipheth-driver</Package>

        <!-- Replaced by python-gtksourceview -->
        <Package>python-pygtksourceview</Package>

        <!-- Replaced by python-paramiko -->
        <Package>paramiko</Package>

        <!-- Replaced by avahi-ui-tools -->
        <Package>python-avahi</Package>

        <!-- Replaced by python-lxml -->
        <Package>lxml</Package>

        <!-- Replaced by python-lxml-docs -->
        <Package>lxml-docs</Package>

        <!-- Texlive language packages are renamed to texlive-lang-* -->
        <Package>texlive-langafrican</Package>
        <Package>texlive-langarab</Package>
        <Package>texlive-langarmenian</Package>
        <Package>texlive-langcjk</Package>
        <Package>texlive-langcroatian</Package>
        <Package>texlive-langcyrillic</Package>
        <Package>texlive-langczechslovak</Package>
        <Package>texlive-langdanish</Package>
        <Package>texlive-langdutch</Package>
        <Package>texlive-langfinnish</Package>
        <Package>texlive-langfrench</Package>
        <Package>texlive-langgerman</Package>
        <Package>texlive-langgreek</Package>
        <Package>texlive-langhebrew</Package>
        <Package>texlive-langhungarian</Package>
        <Package>texlive-langindic</Package>
        <Package>texlive-langitalian</Package>
        <Package>texlive-langlatin</Package>
        <Package>texlive-langmongolian</Package>
        <Package>texlive-langnorwegian</Package>
        <Package>texlive-langother</Package>
        <Package>texlive-langpolish</Package>
        <Package>texlive-langportuguese</Package>
        <Package>texlive-langspanish</Package>
        <Package>texlive-langswedish</Package>
        <Package>texlive-langtibetan</Package>
        <Package>texlive-langukenglish</Package>
        <Package>texlive-langvietnamese</Package>

        <!-- Replaced by python-icu -->
        <Package>PyICU</Package>

        <!-- Replaced by python-reportlab -->
        <Package>reportlab</Package>

        <!-- Replaced by python-reportlab-docs -->
        <Package>reportlab-docs</Package>

        <!-- Replaced by icon-theme-tango -->
        <Package>tango-icon-theme</Package>

        <!-- Replaced by python-xattr -->
        <Package>pyxattr</Package>

        <!-- Replaced by python-svn -->
        <Package>pysvn</Package>

        <!-- Replaced by python-libacl -->
        <Package>pylibacl</Package>

        <!-- french translation available again in 4.2.0
        <Package>kdevelop-l10n-fr</Package> -->
        <!-- russian translation available again in 4.2.0
        <Package>kdevelop-l10n-ru</Package> -->
        <!--finnish translation disappeared in 4.2.0 -->
        <Package>kdevelop-l10n-fi</Package>
        <!--galician translation disappeared in 4.2.0 -->
        <Package>kdevelop-l10n-gl</Package>

        <!-- Replaced by python-chardet -->
        <Package>chardet</Package>

        <!-- Replaced by python-cherrypy -->
        <Package>cherrypy</Package>

        <!-- Replaced by python-pytz -->
        <Package>pytz</Package>

        <!-- Replaced by python-pytextile -->
        <Package>pytextile</Package>

        <!-- Replaced by python-pyserial -->
        <Package>pyserial</Package>

        <!-- Replaced by python-pyparsing -->
        <Package>pyparsing</Package>

        <!-- Replaced by python-pyfits -->
        <Package>pyfits</Package>

        <!-- Replaced by hmmer-docs -->
        <Package>hmmer-doc</Package>

        <!-- Replaced by python-mpmath -->
        <Package>mpmath</Package>

        <!-- Replaced by python-imdbpy -->
        <Package>imdbpy</Package>

        <!-- Replaced by python-pycha -->
        <Package>pycha</Package>

        <!-- Replaced by python-pydns -->
        <Package>pydns</Package>

        <!-- Replaced by python-gtk -->
        <Package>pygtk</Package>

        <!-- Replaced by python-gtk-demo -->
        <Package>pygtk-demo</Package>

        <!-- Replaced by python-gtk-docs -->
        <Package>pygtk-docs</Package>

        <!-- Replaced by python-gtk-devel -->
        <Package>pygtk-devel</Package>

        <!-- Obsoleted by ffmpefthumbnailer -->
        <Package>mplayerthumbs</Package>

        <!-- Obsoleted by rpcbind -->
        <Package>portmap</Package>

        <!-- Replaced by python-pylons -->
        <Package>pylons</Package>

        <!-- Replaced by kernel-docs -->
        <Package>kernel-doc</Package>

        <!-- Replaced by linux-firmware -->
        <Package>kernel-firmware</Package>
        <Package>accessrunner-firmware</Package>
        <Package>aic94xx-firmware</Package>
        <Package>ar9170-firmware</Package>
        <Package>eagle-firmware</Package>
        <Package>ipw2100-firmware</Package>
        <Package>ipw2200-firmware</Package>
        <Package>iwlwifi1000-ucode</Package>
        <Package>iwlwifi3945-ucode</Package>
        <Package>iwlwifi4965-ucode</Package>
        <Package>iwlwifi5000-ucode</Package>
        <Package>iwlwifi5150-ucode</Package>
        <Package>iwlwifi6000-ucode</Package>
        <Package>iwlwifi6050-ucode</Package>
        <Package>ql2x00-firmware</Package>
        <Package>radeon-rlc-firmware</Package>
        <Package>ralink-firmware</Package>
        <Package>v4l-conexant-firmware</Package>
        <Package>v4l-dvb-firmware</Package>
        <Package>zd1201-firmware</Package>
        <Package>zd1211-firmware</Package>

        <!-- Replaced by kernel -->
        <Package>module-gspca</Package>
        <Package>module-pae-gspca</Package>
        <Package>module-gspca-userspace</Package>

        <Package>module-microdia</Package>
        <Package>module-pae-microdia</Package>
        <Package>module-microdia-userspace</Package>

        <Package>module-ov51x-jpeg</Package>
        <Package>module-pae-ov51x-jpeg</Package>
        <Package>module-ov51x-jpeg-userspace</Package>

        <Package>module-uvcvideo</Package>
        <Package>module-pae-uvcvideo</Package>
        <Package>module-uvcvideo-userspace</Package>

        <Package>module-omnibook</Package>
        <Package>module-pae-omnibook</Package>
        <Package>module-omnibook-userspace</Package>

        <Package>module-lenovo-sl-laptop</Package>
        <Package>module-pae-lenovo-sl-laptop</Package>
        <Package>module-lenovo-sl-laptop-userspace</Package>

        <!-- Replaced with module-compat-kvm-* -->
        <Package>module-kvm</Package>
        <Package>module-pae-kvm</Package>
        <Package>module-kvm-userspace</Package>

        <!-- http://lists.gnu.org/archive/html/qemu-devel/2009-08/msg00680.html -->
        <Package>module-kqemu</Package>
        <Package>module-pae-kqemu</Package>
        <Package>module-kqemu-userspace</Package>

        <!-- Kernel debugging stuff changed and this is not maintained since 2.6.26 -->
        <Package>module-icebox</Package>
        <Package>module-pae-icebox</Package>
        <Package>module-icebox-userspace</Package>

        <!-- Bloatware -->
        <Package>module-slmodem</Package>
        <Package>module-pae-slmodem</Package>
        <Package>module-slmodem-userspace</Package>
        <Package>module-ungrab-winmodem</Package>
        <Package>module-pae-ungrab-winmodem</Package>
        <Package>module-ungrab-winmodem-userspace</Package>
        <Package>module-ltmodem</Package>
        <Package>module-pae-ltmodem</Package>
        <Package>module-ltmodem-userspace</Package>

        <!-- Replaced by xmlsec1-->
        <Package>xmlsec</Package>

        <!-- Replaced by python-epydoc -->
        <Package>epydoc</Package>

        <!-- Replaced by python-epydoc-docs -->
        <Package>epydoc-doc</Package>

        <!-- Replaced by python-sqlalchemy -->
        <Package>sqlalchemy</Package>

        <!-- Replaced by kdeartwork-icons -->
        <Package>nuvola</Package>

        <!-- Replaced by python-protocols -->
        <Package>PyProtocols</Package>

        <!-- Replaced by python-dns -->
        <Package>dnspython</Package>

        <!-- Replaced by python-cherrypy2 -->
        <Package>cherrypy2</Package>

        <!-- Replaced by libqd -->
        <Package>qd</Package>

        <!-- Replaced by xorg-video-fglrx -->
        <Package>ati-control-center</Package>

        <!-- Neither used nor developed anymore -->
        <Package>libmovtar</Package>

        <!-- Replaced by their python-* counterparts -->
        <Package>pyusb</Package>
        <Package>pycups</Package>
        <Package>pysmbc</Package>

        <!-- Replaced by panda-kde -->
        <Package>display-settings</Package>

        <!-- Replaced by python-tgmochikit -->
        <Package>TurboGears-mochikit</Package>

        <!-- Replaced by python-turbogears -->
        <Package>TurboGears</Package>

        <!-- replaced by non-binary named eclipse versions -->
        <Package>eclipse-binary</Package>
        <Package>eclipse-cdt-binary</Package>
        <Package>eclipse-jdt-binary</Package>
        <Package>eclipse-pde-binary</Package>
        <Package>pydev</Package>

        <!-- Replaced by new subpackages of kdebindings like smoke-devel, python-kde-devel etc.-->
        <Package>kdebindings-devel</Package>

        <!-- Turbogears don't use it anymore -->
        <Package>python-turboflot</Package>

        <!-- Replaced by bluedevil -->
        <Package>kdebluetooth</Package>

        <!-- Replaced by python-bluez -->
        <Package>pybluez</Package>

        <!-- Too old/does not work/not needed, etc. -->
        <Package>kernel-source</Package>
        <Package>uhinv</Package>
        <Package>arrayprobe</Package>
        <Package>interbench</Package>
        <Package>acx-firmware</Package>
        <Package>fnfx</Package>
        <Package>acpi4asus</Package>
        <Package>hgforest</Package>

        <!-- WQ3 team handed the development over to the SmokinGuns -->
        <Package>westernquake3</Package>
        <Package>westernquake3-data</Package>

        <!-- KVM is merged into qemu package -->
        <Package>kvm</Package>

        <!-- Replaced by python-egenix-mx-base -->
        <Package>egenix-mx-base</Package>

        <!-- Replaced by zemberek-office -->
        <Package>zemberek-openoffice</Package>

        <!-- pt-BR packages are replaced by pt_BR packages -->
        <Package>libreoffice-langpack-pt-BR</Package>
        <Package>libreoffice-help-pt-BR</Package>

        <!-- Replaced by gtk-style-oxygen -->
        <Package>gtk2-themes-kde4oxygen</Package>

        <!-- replaced by qt-docs and qt-docs-html -->
        <Package>qt-doc</Package>
        <Package>qt-doc-html</Package>

        <!-- merged inte kdebase-workspace -->
        <Package>kwin-aurorae-style</Package>

        <!-- plasmoid-X -> plasma-applet-X -->
        <Package>plasmoid-yawp</Package>
        <Package>plasmoid-daisy</Package>
        <Package>plasmoid-message-indicator</Package>

        <!-- Documentation disabled -->
        <Package>vigra-docs</Package>
        <Package>konqueror-plugins-doc</Package>
        <Package>cppunit-doc</Package>

        <!-- replaced by kde-thumbnailer-ffmpeg from kdemultimedia -->
        <Package>kffmpegthumbnailer</Package>

        <!-- No longer supported -->
        <Package>kde-l10n-bn_IN</Package>
        <Package>kde-l10n-csb</Package>
        <Package>kde-l10n-ku</Package>
        <Package>kde-l10n-mk</Package>
        <Package>kde-l10n-tg</Package>

        <!-- No longer supported -->
        <Package>libproxy-webkit-gtk</Package>
        <Package>libproxy-mozjs</Package>

        <!-- Replaced by python-Notifier -->
        <Package>pyNotifier</Package>

        <!-- Replaced by python-Mbus -->
        <Package>python-pyMbus</Package>

        <!-- package renamed to libwbxml -->
        <Package>wbxml2</Package>
        <Package>wbxml2-devel</Package>

        <!-- package renamed to plasma-applet-adjustable-clock -->
        <Package>plasmoid-adjustable-clock</Package>

        <!-- package renamed to plasma-applet-daisy -->
        <Package>plasmoid-daisy</Package>

        <!-- package renamed to plasma-applet-translatoid -->
        <Package>plasmoid-translatoid</Package>

        <!-- Not provided anymore -->
        <Package>openjpeg-doc</Package>
        <Package>gimp-i18n</Package>

        <!-- Replaced by python-lzma -->
        <Package>pylzma</Package>

        <!-- Replaced by libicns* -->
        <Package>icns2png</Package>
        <Package>icns2png-devel</Package>

        <!-- erlang is renamed to erlang-suite -->
        <Package>erlang</Package>

        <!-- will come with texlive-bin in the future -->
        <Package>mplib</Package>
        <Package>xindy</Package>

        <!-- Replaced by python-PyX -->
        <Package>PyX</Package>

        <!-- Replaced by hplip-docs -->
        <Package>hplip-doc</Package>

        <!-- Replaced by python-PyXML -->
        <Package>PyXML</Package>

        <!-- Replaced by python-fpconst -->
        <Package>fpconst</Package>

        <!-- Replaced by python-SOAPpy -->
        <Package>SOAPpy</Package>

        <!-- Replaced by python-aggdraw -->
        <Package>aggdraw</Package>

        <!-- Replaced by python-feedparser -->
        <Package>feedparser</Package>

        <!-- Replaced by python-gdmodule -->
        <Package>gdmodule</Package>

        <!-- Replaced by python-gnuplot-py -->
        <Package>gnuplot-py</Package>

        <!-- Replaced by python-httplib2 -->
        <Package>httplib2</Package>

        <!-- Replaced by python-matplotlib -->
        <Package>matplotlib</Package>

        <!-- Replaced by python-numpy -->
        <!-- More info on: http://www.stsci.edu/resources/software_hardware/numarray -->
        <Package>numarray</Package>

        <!-- Replaced by python-pexpect -->
        <Package>pexpect</Package>

        <!-- Replaced by python-pycallgraph -->
        <Package>pycallgraph</Package>

        <!-- Replaced by python-pydelicious -->
        <Package>pydelicious</Package>

        <!-- Replaced by python-pylirc -->
        <Package>pylirc</Package>

        <!-- Replaced by python-zope-interface-->
        <Package>ZopeInterface</Package>

        <!-- Replaced by python-scapy -->
        <Package>scapy</Package>

        <!-- Replaced by python-graphviz -->
        <Package>pygraphviz</Package>

        <!-- Replaced by python-scipy -->
        <Package>scipy</Package>

        <!-- Replaced by python-networkx -->
        <Package>networkx</Package>

        <!-- Replaced by python-zodb3-->
        <Package>zodb3</Package>

        <!-- Replaced by python-zope-proxy-->
        <Package>zope-proxy</Package>

        <!-- Replaced by python-fltk-->
        <Package>pyFltk</Package>

        <!-- Replaced by python-4Suite-->
        <Package>4suite</Package>

        <!-- Replaced by python-nifti-->
        <Package>pynifti</Package>

        <!-- This is not needed in 2011 -->
        <Package>tuxonice-userui</Package>

        <!-- Very RPM and Fedora specific technology -->
        <Package>readahead</Package>

        <!-- Drop this binary EPSON printer driver -->
        <Package>epson-alc1100</Package>

        <!-- Replaced by qwtplot3d-->
        <Package>qwtplot3d-qt4</Package>

        <!-- It doesn't support Python 2.7 and is also non x86-64 compatible -->
        <Package>psyco</Package>

        <!-- cmemcache is retired  -->
        <Package>cmemcache</Package>

        <!-- Not used anymore -->
        <Package>gptsync</Package>

        <!-- Deprecated, no ones use it-->
        <Package>lpc10</Package>

        <!-- Replaced by mono-* -->
        <Package>mono</Package>

        <!--
            ************************************************************
            not gone to binary stable yet, please don't remove this mark
            ************************************************************
        -->

        <!-- Not provided anymore by opensc 0.12.0 -->
        <Package>opensc-devel</Package>
        <Package>mozilla-opensc-signer</Package>

        <!-- Replaced by yali -->
        <Package>yali4</Package>

        <!-- Replaced by python-sympy-->
        <Package>sympy</Package>

        <!-- Replaced by python-xmpppy-->
        <Package>xmpppy</Package>

        <!-- Replaced by atomicparsley-->
        <Package>AtomicParsley</Package>

        <!-- Replaced by blobby2-->
        <Package>blobby</Package>

        <!-- Replaced by python-kiwi -->
        <Package>kiwi</Package>

        <!-- Replaced by python-inotify -->
        <Package>pyinotify</Package>

        <!-- Replaced by python-inotify-docs -->
        <Package>pyinotify-doc</Package>

        <!-- Replaced by python-webkit-gtk -->
        <Package>pywebkitgtk</Package>

        <!-- Replaced by vnc2flv -->
        <Package>vnc2swf</Package>
    </Obsoletes>
</PISI>
