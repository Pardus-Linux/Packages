diff -Nur ffmpeg-old/libavcodec/x86/cpuid.c ffmpeg/libavcodec/x86/cpuid.c
--- ffmpeg-old/libavcodec/x86/cpuid.c	2010-08-19 20:15:24.216523432 +0300
+++ ffmpeg/libavcodec/x86/cpuid.c	2010-08-19 20:21:53.267658353 +0300
@@ -26,6 +26,8 @@
 
 #undef printf
 
+static int caps_printed = 0;
+
 /* ebx saving is necessary for PIC. gcc seems unable to see it alone */
 #define cpuid(index,eax,ebx,ecx,edx)\
     __asm__ volatile\
@@ -121,8 +123,10 @@
         if (rval & FF_MM_SSE3) rval ^= FF_MM_SSE3SLOW|FF_MM_SSE3;
     }
 
-#if 0
-    av_log(NULL, AV_LOG_DEBUG, "%s%s%s%s%s%s%s%s%s%s%s%s\n",
+
+    if(!caps_printed) {
+        caps_printed = 1;
+    av_log(NULL, AV_LOG_INFO, "CPU Flags detected: %s%s%s%s%s%s%s%s%s%s%s%s\n",
         (rval&FF_MM_MMX) ? "MMX ":"",
         (rval&FF_MM_MMX2) ? "MMX2 ":"",
         (rval&FF_MM_SSE) ? "SSE ":"",
@@ -135,7 +139,8 @@
         (rval&FF_MM_SSE42) ? "SSE4.2 ":"",
         (rval&FF_MM_3DNOW) ? "3DNow ":"",
         (rval&FF_MM_3DNOWEXT) ? "3DNowExt ":"");
-#endif
+    }
+
     return rval;
 }
 
